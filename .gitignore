node_modules
dist
.next
out
*.tsbuildinfo
.DS_Store
*.local
smoke.png
smoke-next.png
offline-next.png
# Rendered by `npm run og` on every build.
/public/og-image.png

# One share card per listing, rendered by `npm run share-cards`.
/public/share

/public/maplibre-worker.mjs
/public/maplibre-gl-shared.mjs

# Vendored from iBurn-Data by `npm run fetch-data` — not ours to redistribute.
/public/data
/public/fonts

# Local secrets: BURNING_MAN_API_KEY and friends.
.env
.env.*
!.env.example

# Editor-local debug config, not repo config — .claude/settings.json and
# .claude/hooks/ are the tracked, shared parts of this directory.
.claude/launch.json
